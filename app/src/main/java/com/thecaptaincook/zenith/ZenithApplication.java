package com.thecaptaincook.zenith;

import android.app.Application;

public class ZenithApplication extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
        // DynamicColors removed — Zenith uses its own custom multi-theme system.
    }
}
